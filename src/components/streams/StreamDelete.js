import React from 'react';
import Modal from '../Modal';

const StreameDelete = () => {
    return (
        <div>
             StreameDelete
             <Modal />
        </div>
    );
};

export default StreameDelete;